type: "object"
properties:
  self:
    type: "string"
    format: "uri"
    description: "URI of the password reset resource"
  status:
    type: "string"
    enum: ["Pending","Completed","Cancelled"]
    description: "Current status of the password reset"