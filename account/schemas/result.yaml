type: "object"
properties:
  identification:
    type: "string"
    description: "The username or email address provided for the account lookup"
  role:
    type: "string"
    description: "The role provided for the account lookup."
  name:
    type: "string"
    description: "The name linked to the provided identification ,if one was found."
  found:
    type: "boolean"
    description: "Whether or not an account matching the provided identification and role was found."