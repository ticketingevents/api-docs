type: "object"
required:
  - password
  - code
properties:
  password:
    type: "string"
    description: "The value the password should be reset to."
  code:
    type: "string"
    description: "The reset code sent to the account email address."