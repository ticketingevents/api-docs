description: "The account password has already been reset, or the reset request has expired."
content:
  application/json:
    schema:
      "$ref": "../../responses/message.yaml"
    example:
      message: "The password reset request has already been completed or expired."