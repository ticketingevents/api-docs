description: |
  The supplied password reset code was incorrect.
content:
  application/json:
    schema:
      "$ref": "../../responses/message.yaml"
    example:
      message: "The provided password reset code is incorrect."