description: "The specified username or email address is already associated with an existing account"
content:
  application/json:
    schema:
      "$ref": "../../responses/message.yaml"
    example:
      message: "The following arguments conflict with those of another user: username."