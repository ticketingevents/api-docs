description: "No email address was provided with the account verification request."
content:
  application/json:
    schema:
      "$ref": "../../responses/message.yaml"
    example:
      message: "You must provide an email address to which to send the verification code."