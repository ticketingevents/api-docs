description: "No account was found matching the provided username or email address"
content:
  application/json:
    schema:
      "$ref": "../../responses/message.yaml"
    example:
      message: "The requested account could not be located"