description: "The approved parameter was omitted from the query."
content:
  application/json:
    schema:
      "$ref": "../../../responses/message.yaml"
    example:
      message: "You must indicate whether or not to approve the submission"