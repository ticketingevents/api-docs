description: "One or more of the specified sections do not exist or the sender does not own sufficient tickets to complete the transfer."
content:
  application/json:
    schema:
      "$ref": "../../../responses/message.yaml"
    example:
      message: "The sender does not hold sufficient itckets in their wallet to complete this transfer."