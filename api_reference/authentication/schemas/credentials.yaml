type: "object"
required:
  - identification
  - password
properties:
  identification:
    type: "string"
    description: "The username or email address of the account starting the session"
  password:
    type: "string"
    format: "password"
    description: "The password of the account starting the session"