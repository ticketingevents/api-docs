description: |
  The supplied verification code was incorrect.
content:
  application/json:
    schema:
      "$ref": "../../responses/message.yaml"
    example:
      message: "The provided verification code is invalid."