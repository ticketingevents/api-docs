description: "The reason for toggling the account was omitted or invalid."
content:
  application/json:
    schema:
      "$ref": "../../responses/message.yaml"
    example:
      message: "The following arguments have invalid values: reason."