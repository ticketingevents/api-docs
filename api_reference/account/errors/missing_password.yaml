description: "A new account password has not been provided."
content:
  application/json:
    schema:
      "$ref": "../../responses/message.yaml"
    example:
      message: "You must provide a new password to initiate a reset."