description: "One or more of the specified sections do not exist or has insufficient availability to satisty the order."
content:
  application/json:
    schema:
      "$ref": "../../responses/message.yaml"
    example:
      message: "The specified section does not have sufficient capacity remaining"