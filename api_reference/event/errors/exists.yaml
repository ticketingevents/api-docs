description: "An event with the specified title already exists in the system"
content:
  application/json:
    schema:
      "$ref": "../../responses/message.yaml"
    example:
      message: "The following arguments conflict with those of another host: title."