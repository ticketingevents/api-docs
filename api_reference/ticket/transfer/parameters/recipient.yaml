in: query
name: recipient
schema:
  type: string
  pattern: "^[A-Z]{2}-[A-Z0-9]{8}$"
required: false
description: "If set, only returns transfers sent to the customer identified by the specified account number"