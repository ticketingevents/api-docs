description: "An event category with the specified name already exists in the system"
content:
  application/json:
    schema:
      "$ref": "../../../responses/message.yaml"
    example:
      message: "The following arguments conflict with those of another category: name."