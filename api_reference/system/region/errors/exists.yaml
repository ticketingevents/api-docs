description: "The provided name is already in use by another region"
content:
  application/json:
    schema:
      "$ref": "../../../responses/message.yaml"
    example:
      error: "The following arguments conflict with those of another region: name."