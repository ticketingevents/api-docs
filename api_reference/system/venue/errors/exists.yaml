description: "The provided name is already in use by another venue in this region"
content:
  application/json:
    schema:
      "$ref": "../../../responses/message.yaml"
    example:
      error: "The following arguments conflict with those of another venue: name."