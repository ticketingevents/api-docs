description: "The requested results page does not exist for the number of records specified."
content:
  application/json:
    schema:
      "$ref": "../../responses/message.yaml"
    example:
      error: "The specified page does not exist for the given records per page."