description: "The order was not successfully fulfilled or has already been refunded."
content:
  application/json:
    schema:
      "$ref": "../../../responses/message.yaml"
    example:
      message: "You cannot refund a cancelled order."