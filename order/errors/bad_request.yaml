description: "One or more of the specified tiers do not exist, have insufficient availability or are complimentary, or the requested ticket amount exceeds a tier's purchase limit."
content:
  application/json:
    schema:
      "$ref": "../../responses/message.yaml"
    example:
      message: "The specified tier does not have sufficient capacity remaining"