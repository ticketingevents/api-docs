description: "An event category with the specified name already exists in the system."
content:
  application/json:
    schema:
      "$ref": "../../../responses/message.yaml"
    example:
      error: "Creating the requested Category would violate uniqueness constraints."