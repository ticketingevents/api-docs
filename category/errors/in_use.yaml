description: "One or more events are currently listed under this category"
content:
  application/json:
    schema:
      "$ref": "../../../responses/message.yaml"
    example:
      error: "The requested category is in use and cannot be deleted."