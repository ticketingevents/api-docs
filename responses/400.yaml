description: "The request payload is invalid JSON or has missing, malformed or incorrect attributes."
content:
  application/json:
    schema:
      "$ref": "message.yaml"
    example:
      error: "Your request payload is invalid. Please ensure you have included all required fields and values are well-formed."