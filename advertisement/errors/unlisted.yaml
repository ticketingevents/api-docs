description: "The authenticating user is not authorised to access unlisted ads from this host."
content:
  application/json:
    schema:
      "$ref": "../../responses/message.yaml"
    example:
      message: "You are not authorised to access this unlisted advertisement."