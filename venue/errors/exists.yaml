description: "The provided name is already in use by another venue in this region"
content:
  application/json:
    schema:
      "$ref": "../../../responses/message.yaml"
    example:
      error: "Creating the requested Venue would violate uniqueness constraints."