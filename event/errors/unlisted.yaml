description: "Access to unlisted events is forbidden."
content:
  application/json:
    schema:
      "$ref": "../../responses/message.yaml"
    example:
      message: "You are not authorised to access this unlisted event."