description: "The requesting user is not authorised to perform this operation on events managed by the specified host."
content:
  application/json:
    schema:
      "$ref": "../../responses/message.yaml"
    example:
      message: "You are restricted from perfoming this operation on events managed by the specified host."