description: "The global admission token cannot be deleted."
content:
  application/json:
    schema:
      "$ref": "../../,,/../../responses/message.yaml"
    example:
      message: "The requested resource could not be located."