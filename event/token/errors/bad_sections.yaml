description: "One or more of the specified sections does not belong to this event"
content:
  application/json:
    schema:
      "$ref": "../../,,/../../responses/message.yaml"
    example:
      message: "One or more of the specified sections does not belong to this event."