description: "One or more of the specified events already has a tier with the given name."
content:
  application/json:
    schema:
      "$ref": "../../../responses/message.yaml"
    example:
      message: "One or more of the specified events already has a tier with the given name."