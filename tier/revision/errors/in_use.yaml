description: "Tickets have already been issued under this tier and changes or deletion are no longer possible."
content:
  application/json:
    schema:
      "$ref": "../../responses/message.yaml"
    example:
      message: "A tier with issued tickets can no longer be modified or deleted."