description: "The named API integration token already exists"
content:
  application/json:
    schema:
      "$ref": "../../responses/message.yaml"
    example:
      message: "The following arguments conflict with those of another token: name."