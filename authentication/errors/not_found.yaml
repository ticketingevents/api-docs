description: "No account was found matching the provided identification string"
content:
  application/json:
    schema:
      "$ref": "../../responses/message.yaml"
    example:
      message: "The requested account could not be located."