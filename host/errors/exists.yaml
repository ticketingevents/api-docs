description: "An event host with the specified name already exists in the system"
content:
  application/json:
    schema:
      "$ref": "../../responses/message.yaml"
    example:
      error: "Creating the requested Host would violate uniqueness constraints."